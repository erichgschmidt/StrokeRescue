import { ReactElement } from "react";

export function OutputOptionsPanel(): ReactElement {
  // TODO: choose output style (masked layer / isolated layer / smart object) + commit.
  return <div />;
}
