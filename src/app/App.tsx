import { ReactElement } from "react";

export function App(): ReactElement {
  return (
    <div style={{ padding: 12 }}>
      <h2 style={{ margin: 0, fontSize: 14, fontWeight: 700 }}>Stroke Rescue</h2>
      {/* TODO: mount SnapshotPanel / ComparePanel / PreviewPanel / OutputOptionsPanel */}
    </div>
  );
}
